export interface IHotel {
    id: string
    name: string
    city: string
    country: string
    postCode: string
    address: string
    description?: string
}

export interface IFormInputs {
    name: string
    city: string
    country: string
    postCode: string
    address: string
    description?: string
}
